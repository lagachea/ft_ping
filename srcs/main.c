#include "ft_ping.h"
#include "libft.h"

int	main(void)
{
	return 0;
}

// parse opts
// prepare packet
// send packet
// on response calc round trip and print it
// again to send packet (need prepare packet again?)
